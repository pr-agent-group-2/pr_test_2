# Todo API (FastAPI + SQLModel)

## Features
* CRUD endpoints
* SQLite persistence
* Pagination (`/todos?offset=0&limit=20`)

## Quick start
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
